rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Default deny all direct client writes
    // All writes go through backend service account
    match /{document=**} {
      allow read: if false;
      allow write: if false;
    }
  }
}
